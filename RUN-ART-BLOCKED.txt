Blocked while fetching the three requested Scenario PNGs.

Tried the authenticated Scenario asset-ID metadata endpoint:
GET https://api.cloud.scenario.com/v1/assets/{asset-id}
Result: HTTP 200 JSON metadata only; it provides an asset.url signed CDN URL, which this task explicitly prohibits using.

Tried authenticated asset-ID binary routes for asset_5RMTqf3TzdAuehY7kMRReksj:
- /download — HTTP 403
- /file — HTTP 403
- /content — HTTP 403
Exact response body (credential value redacted): {"message":"Authorization header requires 'Credential' parameter. Authorization header requires 'Signature' parameter. Authorization header requires 'SignedHeaders' parameter. Authorization header requires existence of either a 'X-Amz-Date' or a 'Date' header. (Hashed with SHA-256 and encoded with Base64) Authorization=[redacted]"}

I need Scenario to provide an authenticated asset-ID binary download endpoint or a supported non-expiring download authorization method, and the Scenario API owner/provider must supply or enable it.